{% extends 'base.html.twig' %}

{% block title %}Espace de suppression des animaux{% endblock %}

{% block body %}
    <h1>Veuillez confirmé la suppression
        {% if(animal.nom is not null) %}
            du <b>{{ animal.nom }}</b>
        {% endif %}
    </h1>
    <br>
    {{ form(formulaire) }}
    <br>
    <a href="{{ path('app_animals') }}" class="btn btn-secondary">retour</a>
{% endblock %}